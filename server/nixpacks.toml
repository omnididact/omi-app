[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm ci --only=production --no-optional",
  "npm cache clean --force"
]

[phases.build]
cmds = ["echo 'Build phase completed'"]

[start]
cmd = "npm start" 